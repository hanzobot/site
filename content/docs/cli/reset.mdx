---
title: "`bot reset`"
description: "CLI reference for `bot reset` (reset local state/config)"
---

# `bot reset`

Reset local config/state (keeps the CLI installed).

```bash
bot reset
bot reset --dry-run
bot reset --scope config+creds+sessions --yes --non-interactive
```
