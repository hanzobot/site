---
title: "`bot uninstall`"
description: "CLI reference for `bot uninstall` (remove gateway service + local data)"
---

# `bot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
bot uninstall
bot uninstall --all --yes
bot uninstall --dry-run
```
