---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Playground — Hanzo Bot" description="A Figma-like collaborative canvas for orchestrating AI agents. See all your bots on an infinite canvas, drill into any bot for terminal, chat, desktop, and files.">
  <div class="stars"></div>
  <div class="nebula"></div>

  <main class="container">
    <!-- Hero -->
    <header class="page-hero">
      <span class="badge">Now Live at app.hanzo.bot</span>
      <h1 class="page-title">Hanzo Playground</h1>
      <p class="page-subtitle">A Figma-like canvas for orchestrating AI agents</p>
      <p class="page-desc">
        See all your bots on an infinite canvas. Drill into any bot for its terminal, chat, desktop, and files.
        Provision entire teams with one click. Approve tool calls from a centralized UI.
      </p>
      <div class="hero-actions">
        <a href="https://app.hanzo.bot/ui/" class="btn-primary" target="_blank" rel="noopener">
          Open Playground
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="16" height="16"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
        </a>
        <a href="https://github.com/hanzoai/playground" class="btn-secondary" target="_blank" rel="noopener">
          <svg viewBox="0 0 24 24" fill="currentColor" width="16" height="16"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
          View Source
        </a>
      </div>
    </header>

    <!-- Canvas Preview (CSS mockup) -->
    <section class="canvas-preview">
      <div class="canvas-mockup">
        <div class="mockup-header">
          <div class="mockup-dots">
            <span class="dot red"></span>
            <span class="dot yellow"></span>
            <span class="dot green"></span>
          </div>
          <span class="mockup-title">Hanzo Playground &mdash; app.hanzo.bot</span>
        </div>
        <div class="mockup-body">
          <div class="mockup-sidebar">
            <div class="sb-item active">
              <span class="sb-icon">&#x25CB;</span>
              <span class="sb-label">Playground</span>
            </div>
            <div class="sb-item">
              <span class="sb-icon">&#x25A1;</span>
              <span class="sb-label">Dashboard</span>
            </div>
            <div class="sb-item">
              <span class="sb-icon">&#x25B7;</span>
              <span class="sb-label">Workflows</span>
            </div>
            <div class="sb-item">
              <span class="sb-icon">&#x2630;</span>
              <span class="sb-label">Teams</span>
            </div>
          </div>
          <div class="mockup-canvas">
            <div class="grid-bg"></div>
            <div class="mock-node node-1">
              <span class="node-status busy"></span>
              <span class="node-name">dev-agent</span>
              <span class="node-model">claude-opus-4-6</span>
            </div>
            <div class="mock-node node-2">
              <span class="node-status idle"></span>
              <span class="node-name">qa-agent</span>
              <span class="node-model">gpt-5.2</span>
            </div>
            <div class="mock-node node-3">
              <span class="node-status busy"></span>
              <span class="node-name">ops-agent</span>
              <span class="node-model">qwen3-235b</span>
            </div>
            <div class="mock-node node-starter">
              <span class="starter-plus">+</span>
              <span class="starter-label">Add a bot</span>
            </div>
            <svg class="mock-edges" viewBox="0 0 600 300">
              <path d="M180,80 C260,80 260,160 340,160" stroke="rgba(255,255,255,0.15)" stroke-width="2" fill="none" stroke-dasharray="4 4"/>
              <path d="M180,80 C260,80 260,240 340,240" stroke="rgba(255,255,255,0.1)" stroke-width="2" fill="none" stroke-dasharray="4 4"/>
            </svg>
            <div class="mock-pill">
              <span class="pill-tool">bash: npm test</span>
              <span class="pill-actions">
                <span class="pill-approve">Approve</span>
                <span class="pill-deny">Deny</span>
              </span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Grid -->
    <section class="features">
      <h2 class="section-title">
        <span class="accent-mark">&#x27E9;</span> Everything in One Canvas
      </h2>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
          </div>
          <h3 class="feature-title">Infinite Canvas</h3>
          <p class="feature-desc">ReactFlow-powered infinite canvas with drag, zoom, pan, snap-to-grid, and minimap. See every agent at a glance.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon icon-cyan">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/></svg>
          </div>
          <h3 class="feature-title">Live Terminal</h3>
          <p class="feature-desc">xterm.js terminal with WebGL rendering. Watch agents execute commands in real-time with 10K line scrollback.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
          </div>
          <h3 class="feature-title">Streaming Chat</h3>
          <p class="feature-desc">Chat with any bot. Streaming markdown with syntax highlighting. Send messages, see responses in real-time.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon icon-cyan">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/><path d="M7 8h2m2 0h2m2 0h2"/></svg>
          </div>
          <h3 class="feature-title">Desktop View</h3>
          <p class="feature-desc">noVNC remote desktop for Operative agents. See your bot's screen &mdash; browser, VS Code, or any GUI app.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
          </div>
          <h3 class="feature-title">File Browser</h3>
          <p class="feature-desc">Browse agent workspace files. View diffs, inspect outputs, and download artifacts directly from the canvas.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon icon-cyan">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          </div>
          <h3 class="feature-title">ActionPill</h3>
          <p class="feature-desc">Centralized tool approval UI. Approve, deny, or auto-accept. Keyboard shortcuts: Tab, Enter, Backspace.</p>
        </div>
      </div>
    </section>

    <!-- Team Presets -->
    <section class="teams-section">
      <h2 class="section-title">
        <span class="accent-mark">&#x27E9;</span> One-Click Team Presets
      </h2>
      <p class="section-desc">Provision an entire team of specialized agents with a single click. Each preset comes with pre-configured roles, models, and workflows.</p>
      <div class="team-grid">
        <div class="team-card">
          <span class="team-emoji">&#x1F6E0;</span>
          <span class="team-name">Dev Team</span>
          <span class="team-roles">Frontend, Backend, QA, DevOps</span>
        </div>
        <div class="team-card">
          <span class="team-emoji">&#x1F4DD;</span>
          <span class="team-name">Content Team</span>
          <span class="team-roles">Writer, Editor, SEO, Social</span>
        </div>
        <div class="team-card">
          <span class="team-emoji">&#x1F50D;</span>
          <span class="team-name">Research Team</span>
          <span class="team-roles">Analyst, Data Sci, Reviewer, PM</span>
        </div>
        <div class="team-card">
          <span class="team-emoji">&#x1F3AF;</span>
          <span class="team-name">Growth Team</span>
          <span class="team-roles">Marketing, Analytics, Design, Outreach</span>
        </div>
        <div class="team-card">
          <span class="team-emoji">&#x1F6E1;</span>
          <span class="team-name">Security Team</span>
          <span class="team-roles">Auditor, Pentester, SIEM, IR</span>
        </div>
        <div class="team-card">
          <span class="team-emoji">&#x2699;</span>
          <span class="team-name">Custom Team</span>
          <span class="team-roles">Build your own preset</span>
        </div>
      </div>
    </section>

    <!-- How It Works -->
    <section class="how-section">
      <h2 class="section-title">
        <span class="accent-mark">&#x27E9;</span> How It Works
      </h2>
      <div class="steps-grid">
        <div class="step-card">
          <span class="step-num">01</span>
          <h3 class="step-title">Connect</h3>
          <p class="step-desc">Open app.hanzo.bot. The canvas connects to the Hanzo Bot gateway via WebSocket ZAP protocol.</p>
        </div>
        <div class="step-card">
          <span class="step-num">02</span>
          <h3 class="step-title">See Your Bots</h3>
          <p class="step-desc">All your running agents appear as nodes on the infinite canvas. Cloud agents, local bots, team members.</p>
        </div>
        <div class="step-card">
          <span class="step-num">03</span>
          <h3 class="step-title">Drill Down</h3>
          <p class="step-desc">Click any bot to see its terminal, chat, desktop, and files. Five views per agent, all live.</p>
        </div>
        <div class="step-card">
          <span class="step-num">04</span>
          <h3 class="step-title">Orchestrate</h3>
          <p class="step-desc">Approve tool calls, provision teams, switch permission modes. Command palette with Cmd+K.</p>
        </div>
      </div>
    </section>

    <!-- Keyboard Shortcuts -->
    <section class="shortcuts-section">
      <h2 class="section-title">
        <span class="accent-mark">&#x27E9;</span> Keyboard-First
      </h2>
      <div class="shortcuts-grid">
        <div class="shortcut">
          <kbd>&#x2318;K</kbd>
          <span>Command palette</span>
        </div>
        <div class="shortcut">
          <kbd>Tab</kbd>
          <span>Next approval</span>
        </div>
        <div class="shortcut">
          <kbd>&#x21E7;Tab</kbd>
          <span>Previous approval</span>
        </div>
        <div class="shortcut">
          <kbd>Enter</kbd>
          <span>Approve action</span>
        </div>
        <div class="shortcut">
          <kbd>&#x232B;</kbd>
          <span>Deny action</span>
        </div>
        <div class="shortcut">
          <kbd>Scroll</kbd>
          <span>Zoom canvas</span>
        </div>
      </div>
    </section>

    <!-- Architecture -->
    <section class="arch-section">
      <h2 class="section-title">
        <span class="accent-mark">&#x27E9;</span> Architecture
      </h2>
      <div class="arch-grid">
        <div class="arch-card">
          <h3 class="arch-title">Control Plane</h3>
          <p class="arch-desc">Go binary with embedded React SPA. Runs locally or in Kubernetes. Agent registry, health checks, job routing.</p>
          <span class="arch-tech">Go + Gin + embed</span>
        </div>
        <div class="arch-card">
          <h3 class="arch-title">Web Client</h3>
          <p class="arch-desc">React + TypeScript SPA. ReactFlow canvas, 5 Zustand stores, WebSocket gateway client, xterm.js, noVNC.</p>
          <span class="arch-tech">React + Vite + Zustand</span>
        </div>
        <div class="arch-card">
          <h3 class="arch-title">Gateway</h3>
          <p class="arch-desc">WebSocket ZAP protocol for real-time agent communication. 43+ RPC methods. Events: chat, agent, exec approval.</p>
          <span class="arch-tech">WebSocket + JSON-RPC</span>
        </div>
        <div class="arch-card">
          <h3 class="arch-title">Deployment</h3>
          <p class="arch-desc">DOKS Kubernetes cluster in hanzo namespace. 2 replicas, rolling updates, Let's Encrypt TLS, nginx ingress.</p>
          <span class="arch-tech">K8s + Helm + GHCR</span>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
      <h2 class="cta-title">Ready to orchestrate?</h2>
      <p class="cta-desc">Open the playground and start managing your AI agents visually.</p>
      <div class="cta-actions">
        <a href="https://app.hanzo.bot/ui/" class="btn-primary" target="_blank" rel="noopener">
          Open Playground
        </a>
        <a href="https://docs.hanzo.ai/docs/bot" class="btn-secondary" target="_blank" rel="noopener">
          Read the Docs
        </a>
      </div>
    </section>
  </main>
</Layout>

<style>
  .container {
    position: relative;
    z-index: 1;
    max-width: 960px;
    margin: 0 auto;
    padding: 60px 24px;
    min-height: 100vh;
  }

  /* Badge */
  .badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--cyan-bright);
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 4px 12px;
    border-radius: 20px;
    margin-bottom: 16px;
    letter-spacing: 0.02em;
  }

  /* Hero */
  .page-hero {
    text-align: center;
    padding: 40px 0 48px;
  }

  .page-title {
    font-family: var(--font-display);
    font-size: clamp(2.4rem, 7vw, 3.6rem);
    font-weight: 700;
    letter-spacing: -0.04em;
    line-height: 1.05;
    margin: 0 0 12px;
    background: linear-gradient(135deg, var(--text-primary) 0%, var(--coral-bright) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .page-subtitle {
    font-family: var(--font-display);
    font-size: 1.3rem;
    font-weight: 400;
    color: var(--text-secondary);
    margin: 0 0 16px;
  }

  .page-desc {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--text-secondary);
    max-width: 640px;
    margin: 0 auto 28px;
  }

  .hero-actions {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Buttons */
  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    font-family: var(--font-display);
    font-size: 0.9rem;
    font-weight: 600;
    color: #050810;
    background: #ffffff;
    border-radius: 10px;
    text-decoration: none;
    box-shadow: 0 4px 20px rgba(255, 255, 255, 0.15);
    transition: all 0.25s ease;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 40px rgba(255, 255, 255, 0.25);
    background: #e8e8e8;
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 24px;
    font-family: var(--font-display);
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--text-primary);
    border: 1px solid var(--border-subtle);
    background: rgba(255, 255, 255, 0.03);
    border-radius: 10px;
    text-decoration: none;
    transition: all 0.25s ease;
  }

  .btn-secondary:hover {
    border-color: var(--coral-bright);
    background: rgba(255, 255, 255, 0.03);
    transform: translateY(-2px);
  }

  /* Canvas Preview Mockup */
  .canvas-preview {
    margin: 0 0 64px;
  }

  .canvas-mockup {
    border: 1px solid var(--border-subtle);
    border-radius: 16px;
    overflow: hidden;
    background: var(--bg-surface);
    box-shadow: 0 16px 64px rgba(0, 0, 0, 0.4);
  }

  .mockup-header {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    background: rgba(17, 24, 39, 0.8);
    border-bottom: 1px solid var(--border-subtle);
  }

  .mockup-dots {
    display: flex;
    gap: 6px;
  }

  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }
  .dot.red { background: #ff5f57; }
  .dot.yellow { background: #febc2e; }
  .dot.green { background: #28c840; }

  .mockup-title {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-muted);
  }

  .mockup-body {
    display: flex;
    height: 340px;
  }

  .mockup-sidebar {
    width: 140px;
    border-right: 1px solid var(--border-subtle);
    padding: 12px 8px;
    flex-shrink: 0;
  }

  .sb-item {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 8px 10px;
    border-radius: 8px;
    font-size: 0.75rem;
    color: var(--text-secondary);
    cursor: default;
    transition: all 0.15s ease;
  }

  .sb-item.active {
    color: var(--coral-bright);
    background: rgba(255, 255, 255, 0.04);
  }

  .sb-icon {
    font-size: 0.8rem;
    opacity: 0.7;
  }

  .sb-label {
    font-family: var(--font-display);
    font-weight: 500;
  }

  .mockup-canvas {
    flex: 1;
    position: relative;
    overflow: hidden;
  }

  .grid-bg {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(136, 146, 176, 0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(136, 146, 176, 0.04) 1px, transparent 1px);
    background-size: 24px 24px;
  }

  .mock-node {
    position: absolute;
    padding: 10px 14px;
    border-radius: 10px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.9);
    backdrop-filter: blur(8px);
    display: flex;
    flex-direction: column;
    gap: 3px;
    font-family: var(--font-display);
    transition: all 0.25s ease;
  }

  .mock-node:hover {
    border-color: var(--coral-bright);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(255, 255, 255, 0.08);
  }

  .node-1 { top: 40px; left: 60px; }
  .node-2 { top: 120px; left: 280px; }
  .node-3 { top: 200px; left: 280px; }

  .node-status {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    position: absolute;
    top: 8px;
    right: 8px;
  }

  .node-status.busy {
    background: var(--cyan-bright);
    box-shadow: 0 0 8px var(--cyan-glow);
    animation: pulse 2s ease-in-out infinite;
  }

  .node-status.idle {
    background: var(--text-muted);
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  .node-name {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .node-model {
    font-family: var(--font-mono);
    font-size: 0.6rem;
    color: var(--text-muted);
  }

  .mock-node.node-starter {
    top: 40px;
    left: 480px;
    border-style: dashed;
    border-color: rgba(136, 146, 176, 0.25);
    align-items: center;
    padding: 14px 20px;
    background: transparent;
  }

  .starter-plus {
    font-size: 1.2rem;
    color: var(--text-muted);
    font-weight: 300;
  }

  .starter-label {
    font-size: 0.7rem;
    color: var(--text-muted);
  }

  .mock-edges {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .mock-pill {
    position: absolute;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 8px 14px;
    border-radius: 20px;
    border: 1px solid var(--border-accent);
    background: rgba(10, 15, 26, 0.95);
    backdrop-filter: blur(12px);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    white-space: nowrap;
  }

  .pill-tool {
    color: var(--text-secondary);
  }

  .pill-actions {
    display: flex;
    gap: 6px;
  }

  .pill-approve {
    color: var(--cyan-bright);
    font-weight: 600;
    cursor: default;
  }

  .pill-deny {
    color: var(--coral-bright);
    font-weight: 600;
    cursor: default;
  }

  /* Section Titles */
  .section-title {
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 600;
    margin-bottom: 12px;
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--text-primary);
  }

  .accent-mark {
    color: var(--coral-bright);
    font-weight: 700;
  }

  .section-desc {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 24px;
    max-width: 640px;
  }

  /* Features Grid */
  .features {
    margin-bottom: 64px;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-top: 24px;
  }

  .feature-card {
    padding: 24px;
    border-radius: 14px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    backdrop-filter: blur(8px);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .feature-card:hover {
    border-color: var(--coral-bright);
    transform: translateY(-3px);
    box-shadow: 0 12px 40px rgba(255, 255, 255, 0.05);
  }

  .feature-icon {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.04);
    color: var(--coral-bright);
    margin-bottom: 14px;
  }

  .feature-icon svg {
    width: 20px;
    height: 20px;
  }

  .feature-icon.icon-cyan {
    background: rgba(255, 255, 255, 0.04);
    color: var(--cyan-bright);
  }

  .feature-title {
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 8px;
  }

  .feature-desc {
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--text-secondary);
    margin: 0;
  }

  /* Team Grid */
  .teams-section {
    margin-bottom: 64px;
  }

  .team-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
  }

  .team-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
    padding: 24px 16px;
    border-radius: 14px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    text-align: center;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .team-card:hover {
    border-color: var(--cyan-bright);
    transform: translateY(-3px);
    box-shadow: 0 12px 40px rgba(255, 255, 255, 0.05);
  }

  .team-emoji {
    font-size: 2rem;
    margin-bottom: 4px;
  }

  .team-name {
    font-family: var(--font-display);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .team-roles {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--text-muted);
  }

  /* Steps */
  .how-section {
    margin-bottom: 64px;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 24px;
  }

  .step-card {
    padding: 24px 20px;
    border-radius: 14px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .step-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-2px);
  }

  .step-num {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--coral-bright);
    letter-spacing: 0.05em;
  }

  .step-title {
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 8px 0;
  }

  .step-desc {
    font-size: 0.8rem;
    line-height: 1.5;
    color: var(--text-secondary);
    margin: 0;
  }

  /* Keyboard Shortcuts */
  .shortcuts-section {
    margin-bottom: 64px;
  }

  .shortcuts-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 20px;
  }

  .shortcut {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 16px;
    border-radius: 10px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    font-size: 0.85rem;
    color: var(--text-secondary);
  }

  kbd {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--text-primary);
    background: var(--bg-elevated);
    border: 1px solid var(--border-subtle);
    border-radius: 6px;
    padding: 3px 8px;
    min-width: 28px;
    text-align: center;
  }

  /* Architecture */
  .arch-section {
    margin-bottom: 64px;
  }

  .arch-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-top: 24px;
  }

  .arch-card {
    padding: 24px;
    border-radius: 14px;
    border: 1px solid var(--border-subtle);
    background: rgba(10, 15, 26, 0.6);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .arch-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-2px);
  }

  .arch-title {
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0 0 8px;
  }

  .arch-desc {
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--text-secondary);
    margin: 0 0 12px;
  }

  .arch-tech {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--cyan-bright);
    background: rgba(255, 255, 255, 0.03);
    padding: 3px 8px;
    border-radius: 4px;
  }

  /* CTA */
  .cta-section {
    text-align: center;
    padding: 48px 0 24px;
    border-top: 1px solid var(--border-subtle);
  }

  .cta-title {
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 4vw, 2.2rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    color: var(--text-primary);
    margin: 0 0 12px;
  }

  .cta-desc {
    font-size: 1rem;
    color: var(--text-secondary);
    margin: 0 0 28px;
  }

  .cta-actions {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }

    .team-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .arch-grid {
      grid-template-columns: 1fr;
    }

    .mockup-sidebar {
      display: none;
    }

    .mockup-body {
      height: 260px;
    }

    .shortcuts-grid {
      gap: 8px;
    }
  }

  @media (max-width: 480px) {
    .team-grid {
      grid-template-columns: 1fr;
    }

    .steps-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
