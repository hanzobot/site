# Google Apps Script Web App URL for the giveaway form.
# Deploy the script in app/(home)/_components/giveaway-form.tsx README block
# as a Web App (Anyone access), then paste the /exec URL here.
NEXT_PUBLIC_GIVEAWAY_URL=
